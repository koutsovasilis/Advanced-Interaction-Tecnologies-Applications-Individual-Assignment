dimioutgisa ena video 10'' me emena
ekanaenan fakelo me onoma data kai topothetithike ekei to video

stin sinartisi setup dilwsa to video kai to path tou kai xrisimopoiisa to loop gia na paizei se loupa

telos stin sinartisi draw xrisimopiisa ton kwdika toy exampl 16_2 kai allksa tin metavliti video se movie
http://learningprocessing.com/examples/chp16/example-16-02-ManipulateCapture

void draw() {
  background(255);

  // Tinting using mouse location
  tint(mouseX, mouseY, 255);

  // A video image can also be moved, rotated, tinted, resized just as with a PImage.
  translate(width/2,height/2);
  imageMode(CENTER);
  rotate(PI/4);
  image(movie, 0, 0, mouseX, mouseY);
}